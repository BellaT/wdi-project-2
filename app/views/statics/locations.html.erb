<div class="row" data-equalizer>

<% @locations.each do |location| %>

  <div class="medium-4 medium-offset-2 square columns" style="background-image: url(<%= @festivals.sample.main_image.url %>);" data-equalizer-watch>
    <h2><%= link_to location, location_path(location) %></h2>
  </div>

<% end %>

</div>